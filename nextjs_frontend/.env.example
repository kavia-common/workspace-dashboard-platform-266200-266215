# Frontend configuration
# The orchestrator should set these in .env (do not commit real values).

# Base URL for the FastAPI backend, e.g. https://<host>:3001
NEXT_PUBLIC_API_BASE_URL=

# Optional: site URL used for redirects (if backend auth later needs it)
NEXT_PUBLIC_SITE_URL=
